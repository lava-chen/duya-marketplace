{
  "defaultMode": "read",
  "writeActionsRequireApproval": true,
  "destructiveActionsRequireApproval": true,
  "permissions": [
    { "name": "workspace.read" },
    { "name": "workspace.write" }
  ],
  "tiers": {
    "read": [
      "notion.search",
      "notion.get_page",
      "notion.get_page_property",
      "notion.get_block_children",
      "notion.get_block",
      "notion.list_databases",
      "notion.get_database",
      "notion.query_database",
      "notion.list_comments",
      "notion.list_users"
    ],
    "draft": [],
    "write": [
      "notion.create_page",
      "notion.update_page",
      "notion.update_page_properties",
      "notion.append_block_children",
      "notion.update_block",
      "notion.create_comment",
      "notion.create_database",
      "notion.update_database",
      "notion.create_database_item"
    ],
    "modify": [
      "notion.archive_page",
      "notion.archive_block",
      "notion.update_database_schema"
    ],
    "destructive": [
      "notion.delete_page",
      "notion.delete_block",
      "notion.delete_database",
      "notion.permanent_delete"
    ]
  },
  "notes": [
    "Searching pages, reading blocks, and querying databases run",
    "automatically. Creating or updating a page is write-tier — confirm",
    "before the first write and always show the proposed page title and",
    "parent. Archiving a page is modify-tier because it removes the page",
    "from the default workspace view (Notion's archive is recoverable,",
    "but the page is effectively hidden). Permanently deleting (trashing)",
    "a page or database is destructive-tier — strong explicit",
    "confirmation, every time. Unlisted actions are bumped one tier",
    "above the conservative default per the design doc §6."
  ]
}
