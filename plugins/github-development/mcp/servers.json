{
  "servers": [
    {
      "name": "github",
      "command": "docker",
      "args": ["run", "-i", "--rm", "-e", "GITHUB_PERSONAL_ACCESS_TOKEN", "ghcr.io/github/github-mcp-server"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${setup.githubToken}"
      },
      "envPassthrough": "allowlist",
      "_status": "official-stdio-docker",
      "_authentication": "Runs the official github-mcp-server image via Docker (ghcr.io/github/github-mcp-server). The `-e GITHUB_PERSONAL_ACCESS_TOKEN` flag forwards the token from the process env (injected from setup.githubToken) into the container. Prefer GitHub App installation tokens or OAuth tokens; long-lived PATs are discouraged."
    }
  ]
}
