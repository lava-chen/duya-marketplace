{
  "defaultMode": "read",
  "writeActionsRequireApproval": true,
  "destructiveActionsRequireApproval": true,
  "permissions": [
    { "name": "workspace.read" },
    { "name": "workspace.write" }
  ],
  "tiers": {
    "read": [
      "sentry.search_issues",
      "sentry.get_issue",
      "sentry.list_issue_events",
      "sentry.get_event",
      "sentry.list_tags",
      "sentry.list_breadcrumbs",
      "sentry.list_projects",
      "sentry.list_releases",
      "sentry.get_release",
      "sentry.list_commits",
      "sentry.listSuspects",
      "sentry.getTrace"
    ],
    "draft": [],
    "write": [
      "sentry.create_release",
      "sentry.update_release",
      "sentry.add_comment",
      "sentry.assign_issue",
      "sentry.set_issue_level"
    ],
    "modify": [
      "sentry.archive_issue",
      "sentry.unarchive_issue"
    ],
    "destructive": [
      "sentry.resolve_issue",
      "sentry.ignore_issue",
      "sentry.delete_issue",
      "sentry.delete_release"
    ]
  },
  "notes": [
    "Reading issues, events, stacktraces, tags, breadcrumbs, and",
    "releases runs automatically. Creating a release or commenting on",
    "an issue is write-tier — confirm before the first write. Archiving",
    "an issue is modify-tier because it hides the issue from default",
    "views. Resolving, ignoring, or deleting an issue is",
    "destructive-tier because these actions hide the issue from future",
    "investigations. Unlisted actions are bumped one tier above the",
    "conservative default per the design doc §6."
  ]
}
